docs {
Antrean laporan (admin/root). Filter status, cursor. Sample:
docs/json/bug-report/admin-list.200.json
}

meta {
  name: Admin List Bug Reports
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/admin/bug-reports?status=open&limit=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("200 + envelope list + meta", () => {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an('array');
    expect(res.body.meta).to.have.property('has_more');
  });
}
