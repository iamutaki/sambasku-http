meta {
  name: Logout All Devices
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/auth/logout-all-devices
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status 200", () => {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}
