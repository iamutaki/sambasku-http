docs {
GET /api/v1/users/me - prefill editor profil (display_name + bio).
Auth Bearer wajib.
}

meta {
  name: Get My Profile
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/users/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("200 + display_name + bio", () => {
    expect(res.status).to.equal(200);
    expect(res.body.data.username).to.be.a("string");
    expect(res.body.data.display_name).to.be.a("string");
    expect(res.body.data).to.have.property("bio");
  });
}
