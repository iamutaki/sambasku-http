docs {
Shuffle batch berikutnya: page=2 (cache terpisah dari page=1).
}

meta {
  name: List Share Backgrounds - page 2
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/share/backgrounds?q=makan%20makanan&page=2
  body: none
  auth: none
}

tests {
  test("200 + page 2", () => {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data.page).to.equal(2);
    expect(res.body.data.items).to.be.an('array');
  });
}
