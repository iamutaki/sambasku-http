docs {
Validasi: q kosong → 400 VALIDATION_ERROR.
}

meta {
  name: List Share Backgrounds - empty q
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/share/backgrounds?q=
  body: none
  auth: none
}

tests {
  test("400 validation", () => {
    expect(res.status).to.equal(400);
    expect(res.body.success).to.equal(false);
    expect(res.body.error_code).to.equal('VALIDATION_ERROR');
  });
}
