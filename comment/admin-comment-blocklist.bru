docs {
CRUD blocklist kata (10-api-comment-blocklist.md). Role admin/root.
}

meta {
  name: Admin Comment Blocklist
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/admin/comment-blocklist
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("200 list blocklist", () => {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an("array");
  });
}
