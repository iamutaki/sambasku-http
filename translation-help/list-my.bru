docs {
Riwayat bantuan milik user login. Filter status opsional
(pending_review | published | rejected | taken_down).
Owner melihat url + provider_file_id + public_url.
}

meta {
  name: List My Translation Helps
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/translation-helps/my?limit=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("200 + owner list", () => {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.be.an("array");
    expect(res.body.meta).to.have.property("has_more");
  });
}
